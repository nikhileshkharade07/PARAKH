# Optional Blockchain

Blockchain is isolated from the core application.

Default:

```env
BLOCKCHAIN_ENABLED=false
```

If enabled later, hash a canonical audit record and anchor the hash on Ethereum Sepolia. Do not store sensitive procurement data on-chain. Blockchain must never block the core demo.
