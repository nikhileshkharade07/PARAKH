# Demo data

All data generated by `backend/scripts/seed_demo.py` is **synthetic/demo data**.

Never represent it as real government procurement data.

Run:

```bash
python -m backend.scripts.seed_demo
```
